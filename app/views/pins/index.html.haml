#pins.transitions-enabled
  - @pins.each do |pin|
    .box.panel.panel-default
      = link_to (image_tag pin.avatar.url), pin
      .panel-body
        %h2= link_to pin.title, pin
        %p.user
        Sunmitted by
        =pin.user.email
